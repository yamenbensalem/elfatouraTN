# À ajouter dans le bloc http{} global de nginx (une seule fois, pas par vhost)
# Nécessaire pour la gestion correcte de l'upgrade WebSocket Blazor/SignalR

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
